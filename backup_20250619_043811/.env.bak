# Email Configuration
EMAIL_USERNAME=ai@mli-corp.com
EMAIL_PASSWORD=Ck8c7a054  # For Gmail, use an App Password
EMAIL_SERVER=imap.stackmail.com
EMAIL_PORT=993
EMAIL_USERNAME=ai@mli-corp.com
EMAIL_PASSWORD=Ck8c7a054  # For Gmail, use an App Password
EMAIL_FOLDER=INBOX

# Docker Compose Optimization
COMPOSE_BAKE=true
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# InvoiceNinja Configuration
INVOICE_NINJA_TOKEN=yGYQbVDbTF0N6uPamGTn26FWsSzCjOuKdFxyJAERqgqA7XWtgmxDoBQJ9spm1uhA

# LLM Backend Configuration
LLM_BACKEND=airllm  # or "transformers"

# Model Configuration (Transformers)
MODEL_PATH=/app/gpt-large  # Using container path
MAX_INPUT_LENGTH=1024
MAX_GENERATION_LENGTH=500

# AirLLM Configuration
MODEL_NAME=/app/gpt-large  # Using local model files
MODEL_CACHE_DIR=/app/model_cache  # Using container path
MODEL_OFFLOAD_DIR=/app/offload  # Using container path
TRUST_REMOTE_CODE=true
TORCH_DTYPE=auto  # float16, bfloat16, float32, or auto
MAX_SEQ_LENGTH=4096
USE_GPU=true
GPU_MEMORY_UTILIZATION=0.9
OFFLOAD_LAYERS_RATIO=0.7
OFFLOAD_LAYERS_BUFFER=4
USE_SAFETENSORS=true
USE_FLASH_ATTENTION=false